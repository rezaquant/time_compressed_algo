Data directory
---------------
- `outputs/`: run artifacts, caches, or intermediate results. This path is git-ignored; keep large/generated files here.

Add other subfolders (e.g., `raw/`, `processed/`) as needed for experiments.
